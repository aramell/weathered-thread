---
scope: Weathered Thread storefront: custom-built browsing/storytelling/picker site handing off to Squarespace for payment only
purpose: build-substrate
altitude: feature
updated: 2026-09-14T14:19
---

- (decision) [ADOPTED] Stack ratified from existing repo (brownfield-lite, create-next-app scaffold already committed): Next.js 16.3.5 App Router, React 19.2.8, TypeScript 5, Tailwind CSS v4, ESLint 9. Not re-derived or re-asked.
- (constraint) Inherited from prior party-mode + bmad-ux sessions, binding, not re-derived: (1) No Squarespace headless/embeddable checkout API exists (web-confirmed) -> custom site owns all browsing/storytelling/picker UX; Squarespace owns payment only, reached via link-out from a Checkout Handoff interstitial to the matching Squarespace product page, never a shared cart. (2) Catalog is small: ~10-12 real Comfort Colors garment SKUs, one launch collection 'Sea Isle' with 13 embroidery motifs, garment-to-motif assignment not yet finalized. (3) Photo strategy: placeholder images now (existing flat patch photos composited onto blank garment photos), swapped for real on-garment photography later without UX changes. (4) Mobile-first. (5) UX spines finalized at _bmad-output/planning-artifacts/ux-designs/ux-weathered-thread-2026-09-14/ (DESIGN.md + EXPERIENCE.md) -- source of truth for IA, components, tokens.
- (decision) Product images: repo-static files (public/ or app-colocated) for now, not an external asset store. Revisit if/when real photography needs non-developer updates.
- (question) User's phrase 'it should live on squarespace' is ambiguous -- interpreted as domain registrar/DNS (weatheredthread.com stays registered/managed at Squarespace, DNS records point to wherever the app hosts), NOT the product catalog (which would contradict the already-established no-headless-API boundary). Flagged to user for confirmation rather than silently assumed.
- (version) Vercel Blob pricing (web-confirmed 2026-09-14): Hobby/free tier includes 1GB storage + 10GB transfer/mo, 10K simple + 2K advanced ops/mo. Paid (Pro) usage: $0.023/GB-month storage, $0.05/GB transfer. Critical caveat: Vercel's free Hobby plan may not be used for any revenue-generating project (enforced) -- since Weathered Thread is a commercial storefront, Pro plan ($20/seat/mo) is required regardless of Blob usage.
- (decision) Domain weatheredthread.com registered/managed at Squarespace (registrar/DNS only). Squarespace store/checkout pages live at a subdomain of weatheredthread.com (e.g. shop.weatheredthread.com), not Squarespace's own domain -- preserves same-domain continuity through the Checkout Handoff. Custom Next.js app is hosted separately (root domain or a www/app subdomain, DNS pointed at the chosen host) and owns the entire catalog/browsing/picker experience, per the already-established no-headless-API boundary.
- (decision) Catalog (garments, motifs, SKU<->Squarespace-product-URL mapping, prices) is structured content checked into the repo (TS/JSON data modules), not a CMS or database. Rebuilt on deploy; editing the data file is the admin workflow. Revisit only if catalog/update-cadence grows well past current scale (~10-12 SKUs, one collection at a time).
- (decision) Email capture: Resend (Vercel Marketplace messaging integration, discovered via 'vercel integration discover --category messaging'), wired via a server action/route handler -- no client-exposed API key. Provisioning deferred to build time.
- (decision) Hosting: Vercel Pro (commercial project, Hobby/free plan explicitly disallows revenue-generating use). Rationale: repo is already Next.js App Router, Vercel is the native zero-config host for it; AWS (Amplify or hand-rolled S3/CloudFront/Lambda) would add real ops overhead with no benefit at this traffic/complexity scale. Preview deployments come free with Vercel per-branch, no extra decision needed.
- (direction) Paradigm: Server-Components-first App Router (RSC by default; 'use client' islands only for interactive picker state -- motif selection, garment color/size selection, live preview swap, sticky Add-to-Bag bar). Follows directly from the repo's existing Next.js App Router scaffold and the fully-static catalog data -- no realistic alternative paradigm was weighed given the framework and data-shape decisions already made.
- (question) Deferred, not silently dropped: (1) testing strategy -- no dedicated suite planned for v1 presentational site; revisit once picker/handoff logic has enough branching to warrant unit tests. (2) analytics/observability -- not yet decided, small enough to add later without an architectural fork. (3) whether a second town collection changes the catalog data shape -- current shape (collection -> motifs, garment SKUs, cross-linked) should accommodate it, but not proven until a second collection is real. (4) garment-to-motif assignment, final pricing, final SKUs, real photography -- business/content decisions carried from the UX spine, not architecture's to resolve.
- (decision) REVISED catalog data source (supersedes the earlier hand-typed-JSON framing): Squarespace remains source of truth for commerce facts (price, inventory, SKU, product name) via its Products API + Inventory API (API-key auth, read access -- web-confirmed at developers.squarespace.com/commerce-apis, distinct from the still-nonexistent headless cart/checkout API). A build-time sync script pulls that data into the repo's static catalog content -- not a live runtime API call per visitor request, preserving the no-server-DB simplicity. The repo still directly authors what Squarespace has no model for: motif names, Sea Isle place/story copy, collection grouping, embroidery narrative -- cross-linked to the synced Squarespace product by SKU. Prevents: two independently-maintained sources of price/inventory truth drifting apart.
- (decision) Squarespace catalog sync is manually triggered for v1 (run the sync script, commit/deploy) -- not scheduled/webhook-automated. Automate later (e.g. a Vercel Cron job or Squarespace webhook-triggered revalidation) once update cadence or team size justifies it.
- (decision) [ADOPTED] No user accounts/auth on the custom site -- inherited from the UX spine's explicit launch exclusions (no personalization, no loyalty, no login). Prevents: a story or builder introducing session/auth infrastructure the product doesn't call for.
- (event) Distilled ARCHITECTURE-SPINE.md from memlog: 7 ADs (Squarespace boundary, commerce-source-of-truth sync, static catalog, repo-static images, no-auth, Resend email, domain split), paradigm (RSC-first + client islands), stack table, context/deployment mermaid diagrams, source tree, 6 Deferred items. Reconciled against sources (DESIGN.md, EXPERIENCE.md, website-build-handoff-prd.md) -- no load-bearing requirement found dropped; scope separation (visual/behavioral vs structural) holds cleanly.
- (question) Reviewer gate (version-verification lens) flagged: Squarespace's Inventory/Orders/Transactions APIs (needed by AD-2's sync script) may require Commerce Plus/Advanced plan tier, not confirmed available on Commerce Basic. User believes their plan already covers it but asked to table verification for now rather than block on it -- confirm actual Squarespace plan/API access before building the sync script.
- (event) Reviewer gate: 3 parallel subagents (rubric walker, version-verification, adversarial) + lint_spine.py. Version-verification: clean, all claims confirmed against real sources (package.json, Squarespace dev docs, Vercel pricing/ToS docs), flagged one nuance (Squarespace Inventory/Orders/Transactions APIs may need Commerce Plus/Advanced plan -- user tabled verification, believes plan already covers it). Adversarial: 11 findings, sharpest being garment-vs-product SKU-granularity contradiction and build-time-vs-manual sync contradiction. Rubric walker independently caught the same SKU contradiction plus AD-1 outbound-only wording clashing with AD-2/AD-6, AD-3's SKU-count framing, and the Next.js 16 Cache Components dimension being left silent. All applied to ARCHITECTURE-SPINE.md: AD-2 rewritten (SKU=garment only, product assembled in-repo, synced.ts/authored.ts file split, manual-only sync with own commit + main-tip diff + atomic all-or-nothing write, URL host validation folded into AD-1), AD-4 (closed image-field set), AD-6 (single subscribeEmail Server Action), new AD-8 (ProductPickerShell owns all picker state), Naming/Data-formats/State rows tightened (SKU casing, price-as-cents, pinned env var names), Cache Components explicitly deferred with reasoning, source tree and diagram updated to match. lint_spine.py clean (only known false-positive template-token flags). Added GitHub remote (https://github.com/aramell/weathered-thread.git) to repo, not yet pushed.
- (event) spine finalized
